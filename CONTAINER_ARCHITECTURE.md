# 컨테이너 아키텍처 설명

## 📦 전체 계획: 9개 컨테이너

### 프로덕션 환경 (9개)
```
1. nginx-proxy          - Nginx 리버스 프록시 (SSL/TLS)
2. frontend-app         - React 빌드 정적 파일
3. api-gateway          - Node.js Express
4. data-collector       - Python Flask
5. statistics-service   - Python Flask
6. ml-prediction        - Python Flask
7. user-service         - Spring Boot
8. redis-session        - Redis
9. mysql-db             - MySQL
```

## 🎯 현재 상태: 9개 컨테이너 (프로덕션 준비 완료) ⭐⭐⭐ 100% 완성

### 실행 중인 컨테이너 (9/9) ✅
```
✅ 1. nginx-proxy              - 포트 80, 443 ⭐ NEW
✅ 2. frontend-app             - 포트 3000 (Nginx) ⭐ NEW
✅ 3. api-gateway              - 포트 8000
✅ 4. data-collector-service   - 포트 8001
✅ 5. statistics-service       - 포트 8002
✅ 6. ml-prediction-service    - 포트 8003
✅ 7. user-service             - 포트 8004
✅ 8. redis-session            - 포트 6379
✅ 9. mysql-db                 - 포트 3306 (Docker)
```

### 🎊 모든 컨테이너 정상 작동 중!

---

## 🔄 개발 환경 vs 프로덕션 환경

### 개발 환경 (현재)
- **Frontend**: `npm run dev` (포트 3000)
  - Vite 개발 서버
  - Hot Module Replacement (HMR)
  - 빠른 개발 및 디버깅
  
- **직접 접속**:
  - http://localhost:3000 (Frontend)
  - http://localhost:8001 (Data Collector)
  - http://localhost:8002 (Statistics)
  - http://localhost:8003 (ML Prediction)
  - http://localhost:8000 (API Gateway)

### 프로덕션 환경 (계획)
- **Frontend**: Docker 컨테이너
  - `npm run build` 결과물
  - Nginx로 정적 파일 서빙
  - 최적화된 빌드

- **Nginx 라우팅**:
  - https://example.com → Frontend
  - https://example.com/api/data → Data Collector
  - https://example.com/api/stats → Statistics
  - https://example.com/api/ml → ML Prediction
  - https://example.com/api/user → User Service

---

## 📋 각 컨테이너 역할

### 1. nginx-proxy (프로덕션)
- **역할**: 리버스 프록시, SSL/TLS 종료, 로드 밸런싱
- **기술**: Nginx
- **포트**: 80, 443
- **현재**: 개발 환경에서 제외

### 2. frontend-app (프로덕션)
- **역할**: React 빌드 파일 서빙
- **기술**: Nginx + React build
- **포트**: 3000
- **현재**: Vite dev server로 대체

### 3. api-gateway ✅
- **역할**: API 라우팅, 인증 검증
- **기술**: Node.js + Express
- **포트**: 8000
- **상태**: 실행 중

### 4. data-collector-service ✅
- **역할**: 로또 데이터 크롤링 및 수집
- **기술**: Python + Flask
- **포트**: 8001
- **상태**: 실행 중

### 5. statistics-service ✅
- **역할**: 통계 분석 (빈도, 패턴)
- **기술**: Python + Flask + NumPy
- **포트**: 8002
- **상태**: 실행 중

### 6. ml-prediction-service ✅
- **역할**: 머신러닝 예측 (5가지 방식)
- **기술**: Python + Flask + scikit-learn + XGBoost
- **포트**: 8003
- **상태**: 실행 중

### 7. user-service ✅
- **역할**: 사용자 인증, 회원 관리, JWT 토큰 발급
- **기술**: Spring Boot 3.1.5 + JWT + Spring Security
- **포트**: 8004
- **상태**: 실행 중 ⭐ 구현 완료

### 8. redis-session ✅
- **역할**: 캐시, 세션 관리
- **기술**: Redis 7
- **포트**: 6379
- **상태**: 실행 중

### 9. mysql-db ✅
- **역할**: 데이터베이스 (로또 번호, 사용자, 통계)
- **기술**: MySQL 8.0
- **포트**: 3306
- **데이터**: 1196개 회차 저장
- **상태**: Docker 컨테이너로 실행 중 ⭐ 컨테이너화 완료

---

## 🚀 다음 단계

### Phase 4: User Service 구현
```bash
# 컨테이너 추가
docker-compose up -d user-service
```

### Phase 5: 프로덕션 배포
```bash
# Frontend 빌드 및 컨테이너화
cd frontend && npm run build
docker build -t lotto-frontend .

# Nginx 프록시 추가
docker-compose up -d nginx-proxy

# 전체 9개 컨테이너 실행
docker-compose up -d
```

---

## 📊 현재 시스템 구조

```
┌─────────────────────────────────────┐
│   Frontend (Vite Dev Server)       │
│   http://localhost:3000             │
└─────────────┬───────────────────────┘
              │
              ├─────────────────────────┐
              │                         │
    ┌─────────▼──────────┐    ┌────────▼──────────┐
    │  Data Collector    │    │   Statistics      │
    │   :8001            │    │    :8002          │
    └────────────────────┘    └───────────────────┘
              │                         │
              │                         │
    ┌─────────▼──────────┐    ┌────────▼──────────┐
    │  ML Prediction     │    │   API Gateway     │
    │   :8003            │    │    :8000          │
    └────────────────────┘    └───────────────────┘
              │                         │
              └────────┬────────────────┘
                       │
            ┌──────────▼────────────┐
            │      MySQL :3306      │
            │      Redis :6379      │
            └───────────────────────┘
```

---

**작성일**: 2024-11-05  
**최종 업데이트**: 2024-11-06 15:35  
**버전**: 3.0  
**상태**: 프로덕션 환경 (9개 컨테이너 실행 중) ⭐⭐⭐ 100% 완성
